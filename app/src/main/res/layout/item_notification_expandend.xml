<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/item_notificaton_expanded_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Notification text"
            android:textAppearance="@style/TextAppearance.AppCompat.Notification.Title" />

        <TextView
            android:id="@+id/item_notificaton_expanded_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:alpha="0.5"
            android:ellipsize="end"
            android:justificationMode="inter_word"
            android:text="this is a sample text"
            android:textAppearance="@style/TextAppearance.AppCompat.Notification.Description" />

        <ImageView
            android:id="@+id/item_notificaton_expanded_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="6dp"
            android:scaleType="centerCrop"
            android:visibility="gone" />

    </LinearLayout>

</RelativeLayout>
